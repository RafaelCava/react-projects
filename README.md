```bash
# Para iniciar a aplicação primeiro instale as dependências
$ npm install

# Inicie a aplicação react
$ npm run start

# na aplicação next
$ npm run dev

# Caso possua docker iniciar utilizando docker compose
$ docker compose up -d
```
